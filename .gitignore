# Rust build output
target/

# wasm-pack outputs (rebuilt from source via `npm run build`; the npm
# tarball ships them, but they don't belong in git history)
pkg/
pkg-web/
pkg-node/

# Node
node_modules/

# Tarballs
*.tgz

# Gradle / Android build scratch (validation AAR project + rn package's
# Android module both create these).
android/.gradle/
android/build/
android/local.properties
android/.kotlin/
rn/android/.gradle/
rn/android/build/
rn/android/.kotlin/
rn/android/local.properties

# iOS build scratch (the committed xcframework + Sources/ live alongside,
# but the build script's intermediates do not).
apple/build/
apple/.validation/
apple/.build/
apple/.swiftpm/

# react-native-builder-bob output for the rn package — npm tarball ships
# lib/ but it's regenerated from src/ via `npm run build`.
rn/lib/

# OS / editor cruft
.DS_Store
*.swp
